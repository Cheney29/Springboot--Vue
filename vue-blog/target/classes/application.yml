# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vueblog?charset=utf8mb4&serverTimezone=UTC&useUnicode=true&useSSL=false
    username: root
    password: root
mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

shiro-redis:
  enabled: true
  redis-manager:
    host: 127.0.0.1:6379

cheney:
  jwt:

    secret: f4e2e52034348f86b67cde581c0f9eb5

    expire: 604800
    header: Authorization
server:
  port: 8081

